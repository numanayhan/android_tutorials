<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tra.androidui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <activity android:name=".TextComponents.Phone"></activity>
        <activity android:name=".TextComponents.Email" />
        <activity android:name=".TextComponents.Password_Numeric" />
        <activity android:name=".TextComponents.Password" />
        <activity android:name=".TextComponents.PlainText" />
        <activity android:name=".TextComponents.Text" />
        <activity android:name=".TextViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>